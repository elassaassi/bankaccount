Bank Account Microservice:

1- To start the service, please run the Spring Boot class BankAccountApplication.

2- To document and test the API, you can directly access the Swagger interface:
http://localhost:8080/swagger-ui/index.html#/

3- To check the database data, you can access the H2 Console (in-memory database):
http://localhost:8080/h2-console


4- For the coverage, we are at about 73,3%, 
 -may be the  OperationServiceTest Test could be Failed when launching with test coverage,
 we can re-execute it without any change, it will pass (no more time ˆ_^ !!)